{
  "type": "object",
  "properties": {
    "location": {
      "$ref": "{{model:Location}}"
    },
    "sensorId": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "temperature": {
      "$ref": "{{model:Temperature}}"
    },
    "measureTime": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["location", "temperature", "measureTime"],
  "additionalProperties": false
}