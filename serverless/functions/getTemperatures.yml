handler: src/main/temperature_reader.get_all_temperatures
events:
  - http:
      path: temperatures
      method: get
      parameters:
        querystrings:
          location: false
      documentation: ${file(serverless/documentation/getTemperatures.yml)}